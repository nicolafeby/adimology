import { strategyIdentity, type StrategyId } from '../lib/strategies';
import { atJakartaTime, IDX_SESSION_RULES, type ExchangeCalendar } from '../lib/market-calendar';
import type { IntradayCandle, OfficialAraLimit, StrategyScreeningData } from '../lib/strategy-screening';
import type { StrategyBacktestSignal, StrategyOutcomeArchive } from '../lib/strategy-backtest';
export const source = 'https://fixtures.example.invalid/deterministic-exchange-calendar';
export const availableAt = '2026-08-01T00:00:00Z';
export function calendarFixture(): ExchangeCalendar { return { coverageStart:'2026-09-04',coverageEnd:'2026-09-08',days:['2026-09-04','2026-09-05','2026-09-06','2026-09-07','2026-09-08'].map(date=>({date,isTradingDay:['2026-09-04','2026-09-08'].includes(date),sourceUrl:source,availableAt,rulesVersion:IDX_SESSION_RULES.version,verified:true})) }; }
export function bar(date: string, start: string, end: string, prices: Partial<Pick<IntradayCandle,'open'|'high'|'low'|'close'|'volume'|'tradedValue'>> = {}): IntradayCandle { const endAt=atJakartaTime(date,end); return {sessionDate:date,startAt:atJakartaTime(date,start),endAt,availableAt:endAt,resolution:'intraday',open:100,high:102,low:99,close:101,volume:1_000_000,tradedValue:100_000_000,...prices}; }
export function officialAraFixture(date = '2026-09-04'): OfficialAraLimit { return {symbol:'TEST',sessionDate:date,price:125,sourceType:'exchange',sourceUrl:source,observedAt:availableAt,availableAt,board:'main',referencePrice:100,rulesVersion:'fixture-official-limit-v1'}; }
export function signalFixture(strategyId: StrategyId = 'bpjs', override: Partial<StrategyBacktestSignal> = {}): StrategyBacktestSignal { return { ...strategyIdentity(strategyId),snapshotId:`fixture-${strategyId}`,symbol:'TEST',cutoffAt:atJakartaTime('2026-09-04',strategyId==='bsjp'?'15:05':'09:20'),executionMode:'historical_replay',origin:'fixture',pointInTimeValid:true,backtestEligible:true,entryLow:99,entryHigh:102,stopPrice:95,targetPrice:110,shares:100,officialAra:officialAraFixture(),alreadyTouchedAra:false,...override }; }
export function archiveFixture(candles: IntradayCandle[]): StrategyOutcomeArchive { return {origin:'fixture',candles,calendar:calendarFixture(),asOf:atJakartaTime('2026-09-08','16:00'),coverageStart:atJakartaTime('2026-09-04','09:00'),completeThrough:atJakartaTime('2026-09-08','16:00'),missingIntervals:[],source, historicalUniverseVerified:true,corporateActionsVerified:true}; }
export function screeningFixture(): StrategyScreeningData { return {symbol:'TEST',calendar:calendarFixture(),sessionCoverageFromOpen:true,candles:[bar('2026-09-04','09:00','09:15',{high:104,close:103,volume:10_000_000,tradedValue:1_010_000_000}),bar('2026-09-04','09:15','09:20',{open:103,high:105,close:104,low:102,volume:10_000_000,tradedValue:1_030_000_000})],sameClockVolumes:['2026-08-24','2026-08-25','2026-08-26','2026-08-27','2026-08-28'].map(sessionDate=>({sessionDate,throughTime:'09:20',cumulativeVolume:10_000_000,availableAt})),book:{observedAt:atJakartaTime('2026-09-04','09:20'),availableAt:atJakartaTime('2026-09-04','09:20'),bids:[{price:104,shares:1_000_000}],offers:[{price:105,shares:1_000_000}]},officialAra:officialAraFixture()}; }
